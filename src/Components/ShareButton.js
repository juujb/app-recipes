import React from 'react';

export default function ShareButton({ type, id }) {
  function handleClick() {
    if (type === 'drink') {
      const link = '';
    }
  }
  return (
    <button
    onClick={ }
    >
      <img src="../images/shareIcon.svg" alt="Share Button" />
    </button>
  );
}